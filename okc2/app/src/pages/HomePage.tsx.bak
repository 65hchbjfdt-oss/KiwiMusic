import { useState, useEffect } from 'react';
import { TrendingUp, Disc, Calendar, Zap, ChevronRight } from 'lucide-react';
import { usePlayer } from '@/hooks/usePlayer';
import { GlassCard, GlassButton, GradientBlob } from '@/components/ui-custom';
import type { Track } from '@/types';
import { cn } from '@/lib/utils';

// Mock events data
const mockEvents = [
  {
    id: '1',
    title: 'Рок-н-ролл марафон',
    description: 'Послушайте 3 рок песни и получите эксклюзивный стикер!',
    genre: 'rock',
    sticker_name: 'Рок-звезда',
    progress: 1
  },
  {
    id: '2',
    title: 'Поп-вечеринка',
    description: 'Погрузитесь в мир поп-музыки!',
    genre: 'pop',
    sticker_name: 'Поп-икона',
    progress: 0
  }
];

// Mock albums data
const mockAlbums = [
  { id: '1', name: 'Любимый жанр', artist_name: 'KiwiFlow', image_url: 'https://images.unsplash.com/photo-1493225255756-d9584f8606e9?w=300&h=300&fit=crop' },
  { id: '2', name: 'Новинки', artist_name: 'KiwiFlow', image_url: 'https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=300&h=300&fit=crop' }
];

// Mock tracks
const mockTracks: Track[] = [
  {
    id: '1',
    name: 'Summer Vibes',
    artist_name: 'Chill Artist',
    artist_id: '1',
    duration: 180,
    audio_url: 'https://mp3.chillhop.com/serve.php/?mp3=10075',
    image_url: 'https://images.unsplash.com/photo-1493225255756-d9584f8606e9?w=300&h=300&fit=crop',
    genre: 'chill',
    tags: ['chill', 'lofi'],
    has_lyrics: false,
    play_count: 1250
  },
  {
    id: '2',
    name: 'Night Drive',
    artist_name: 'Synthwave Pro',
    artist_id: '2',
    duration: 240,
    audio_url: 'https://mp3.chillhop.com/serve.php/?mp3=10076',
    image_url: 'https://images.unsplash.com/photo-1514525253161-7a46d19cd819?w=300&h=300&fit=crop',
    genre: 'electronic',
    tags: ['synthwave', 'electronic'],
    has_lyrics: true,
    play_count: 980
  },
  {
    id: '3',
    name: 'Morning Coffee',
    artist_name: 'Jazz Trio',
    artist_id: '3',
    duration: 200,
    audio_url: 'https://mp3.chillhop.com/serve.php/?mp3=10077',
    image_url: 'https://images.unsplash.com/photo-1511192336575-5a79af67a629?w=300&h=300&fit=crop',
    genre: 'jazz',
    tags: ['jazz', 'relax'],
    has_lyrics: false,
    play_count: 750
  },
  {
    id: '4',
    name: 'Urban Dreams',
    artist_name: 'Hip Hop Beats',
    artist_id: '4',
    duration: 195,
    audio_url: 'https://mp3.chillhop.com/serve.php/?mp3=10078',
    image_url: 'https://images.unsplash.com/photo-1571330735066-03aaa9429d89?w=300&h=300&fit=crop',
    genre: 'hiphop',
    tags: ['hiphop', 'beats'],
    has_lyrics: true,
    play_count: 620
  },
  {
    id: '5',
    name: 'Ocean Waves',
    artist_name: 'Nature Sounds',
    artist_id: '5',
    duration: 300,
    audio_url: 'https://mp3.chillhop.com/serve.php/?mp3=10079',
    image_url: 'https://images.unsplash.com/photo-1505118380757-91f5f5632de0?w=300&h=300&fit=crop',
    genre: 'ambient',
    tags: ['ambient', 'nature'],
    has_lyrics: false,
    play_count: 890
  }
];

export function HomePage() {
  const { playTrack } = usePlayer();
  const [isLoading, setIsLoading] = useState(true);
  const [activeEventTab, setActiveEventTab] = useState<'tasks' | 'collection'>('tasks');

  useEffect(() => {
    // Simulate loading
    const timeout = setTimeout(() => {
      setIsLoading(false);
    }, 500);
    return () => clearTimeout(timeout);
  }, []);

  const handlePlayTrack = (track: Track) => {
    playTrack(track, mockTracks);
  };

  return (
    <div className="min-h-screen bg-black pb-32">
      {/* KiwiFlow Section */}
      <section className="relative h-[50vh] overflow-hidden">
        <GradientBlob 
          colors={['#10b981', '#f59e0b', '#ec4899', '#8b5cf6']}
          className="opacity-60"
        />
        
        <div className="relative z-10 h-full flex flex-col items-center justify-center px-6">
          <div className="text-center">
            <div className="flex items-center justify-center gap-3 mb-4">
              <div className="w-1 h-8 bg-emerald-400 rounded-full" />
              <div className="w-1 h-8 bg-emerald-400 rounded-full" />
            </div>
            <h1 className="text-4xl md:text-5xl font-bold text-white mb-4">
              KiwiFlow
            </h1>
            <p className="text-white/70 text-lg mb-6">
              Ваша персональная волна музыки
            </p>
            <GlassButton 
              variant="primary" 
              size="lg"
              onClick={() => handlePlayTrack(mockTracks[0])}
            >
              <Zap className="w-5 h-5 mr-2" />
              Настроить
            </GlassButton>
          </div>
        </div>
      </section>

      <div className="px-4 space-y-8">
        {/* Popular Tracks */}
        <section>
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-xl font-bold text-white flex items-center gap-2">
              <TrendingUp className="w-5 h-5 text-emerald-400" />
              Популярные треки
            </h2>
            <button className="text-emerald-400 text-sm flex items-center gap-1 hover:text-emerald-300 transition-colors">
              Все <ChevronRight className="w-4 h-4" />
            </button>
          </div>

          <div className="space-y-2">
            {isLoading ? (
              Array(5).fill(0).map((_, i) => (
                <div key={i} className="h-16 bg-white/5 rounded-xl animate-pulse" />
              ))
            ) : (
              mockTracks.map((track, index) => (
                <div
                  key={track.id}
                  onClick={() => handlePlayTrack(track)}
                  className="flex items-center gap-3 p-3 bg-white/5 hover:bg-white/10 rounded-xl transition-colors cursor-pointer group"
                >
                  <span className="w-6 text-center text-white/40 text-sm">
                    {index + 1}
                  </span>
                  <img
                    src={track.image_url}
                    alt={track.name}
                    className="w-12 h-12 rounded-lg object-cover"
                  />
                  <div className="flex-1 min-w-0">
                    <p className="text-white font-medium truncate group-hover:text-emerald-400 transition-colors">
                      {track.name}
                    </p>
                    <p className="text-white/50 text-sm truncate">
                      {track.artist_name}
                    </p>
                  </div>
                  <span className="text-white/40 text-sm">
                    {Math.floor(track.duration / 60)}:{(track.duration % 60).toString().padStart(2, '0')}
                  </span>
                </div>
              ))
            )}
          </div>
        </section>

        {/* Albums */}
        <section>
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-xl font-bold text-white flex items-center gap-2">
              <Disc className="w-5 h-5 text-emerald-400" />
              Альбомы для вас
            </h2>
          </div>

          <div className="flex gap-4 overflow-x-auto pb-2 scrollbar-hide">
            {mockAlbums.map((album) => (
              <div
                key={album.id}
                className="flex-shrink-0 w-40 cursor-pointer group"
              >
                <div className="relative aspect-square rounded-2xl overflow-hidden mb-2">
                  <img
                    src={album.image_url}
                    alt={album.name}
                    className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                  />
                  <div className="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
                    <div className="w-12 h-12 bg-emerald-500 rounded-full flex items-center justify-center">
                      <TrendingUp className="w-6 h-6 text-white" />
                    </div>
                  </div>
                </div>
                <p className="text-white font-medium truncate">{album.name}</p>
                <p className="text-white/50 text-sm">{album.artist_name}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Events Section */}
        <section>
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-xl font-bold text-white flex items-center gap-2">
              <Calendar className="w-5 h-5 text-emerald-400" />
              События
            </h2>
          </div>

          {/* Event Tabs */}
          <div className="flex gap-2 mb-4">
            <button
              onClick={() => setActiveEventTab('tasks')}
              className={cn(
                'px-4 py-2 rounded-xl text-sm font-medium transition-all',
                activeEventTab === 'tasks'
                  ? 'bg-emerald-500 text-white'
                  : 'bg-white/5 text-white/60 hover:bg-white/10'
              )}
            >
              Задания
            </button>
            <button
              onClick={() => setActiveEventTab('collection')}
              className={cn(
                'px-4 py-2 rounded-xl text-sm font-medium transition-all',
                activeEventTab === 'collection'
                  ? 'bg-emerald-500 text-white'
                  : 'bg-white/5 text-white/60 hover:bg-white/10'
              )}
            >
              Коллекция
            </button>
          </div>

          {activeEventTab === 'tasks' ? (
            <div className="space-y-3">
              {mockEvents.map((event) => (
                <GlassCard 
                  key={event.id} 
                  className="p-4"
                  variant="green"
                >
                  <div className="flex items-start gap-3">
                    <div className="w-12 h-12 bg-emerald-500/20 rounded-xl flex items-center justify-center flex-shrink-0">
                      <Zap className="w-6 h-6 text-emerald-400" />
                    </div>
                    <div className="flex-1">
                      <h3 className="text-white font-semibold mb-1">{event.title}</h3>
                      <p className="text-white/60 text-sm mb-3">{event.description}</p>
                      
                      {/* Progress */}
                      <div className="flex items-center gap-2">
                        <div className="flex-1 h-2 bg-white/10 rounded-full overflow-hidden">
                          <div 
                            className="h-full bg-emerald-500 rounded-full"
                            style={{ width: `${(event.progress / 3) * 100}%` }}
                          />
                        </div>
                        <span className="text-white/60 text-sm">{event.progress}/3</span>
                      </div>
                      
                      <p className="text-emerald-400 text-sm mt-2">
                        Награда: {event.sticker_name}
                      </p>
                    </div>
                  </div>
                </GlassCard>
              ))}
            </div>
          ) : (
            <GlassCard className="p-6 text-center">
              <div className="w-16 h-16 bg-white/5 rounded-full flex items-center justify-center mx-auto mb-4">
                <Disc className="w-8 h-8 text-white/40" />
              </div>
              <p className="text-white/60">Здесь будут ваши стикеры</p>
              <p className="text-white/40 text-sm mt-1">
                Выполняйте задания, чтобы получать награды
              </p>
            </GlassCard>
          )}
        </section>
      </div>
    </div>
  );
}
